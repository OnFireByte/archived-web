import Block from "@/components/Block";

const Home = () => {
    return (
        <Block className="w-full">
            Dunno what im gonna put in homepage, but another tabs is working doe
        </Block>
    );
};

export default Home;
